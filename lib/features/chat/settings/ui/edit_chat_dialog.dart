import 'package:flutter/widgets.dart';

class EditChatDialog extends StatelessWidget {
  const EditChatDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
